# Configuração do Banco de Dados
# Copie a Internal Database URL do seu PostgreSQL no Render
DATABASE_URL=postgres://usuario:senha@host/database

# Porta do servidor (Render usa PORT automaticamente)
PORT=10000

# Ambiente
NODE_ENV=production
